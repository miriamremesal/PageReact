import React from 'react'
import ReactDOM from 'react-dom/client'
import NavbarTech from './components/Navbar'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <NavbarTech/>
   
  </React.StrictMode>,
)